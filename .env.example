# Server
PORT=5000
MONGO_URI=mongodb://127.0.0.1:27017/mrm_rebuild
JWT_SECRET=<generate-a-long-random-secret>
JWT_EXPIRE=7d
NODE_ENV=development

# Client
# Leave unset for same-origin /api. Set when the API is hosted separately.
VITE_API_URL=http://localhost:5000/api
